{% for post in posts %}
    <div class="nav nav-pills nav-stacked">
        <li role="presentation"><a href="{{ path('post', {'slug': post.slug}) }}">{{ post.title }} ({{ post.commentsCount }})</a></li>
    </div>
{% endfor %}